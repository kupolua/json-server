{
  "name": "json-server",
  "version": "0.0.1",
  "description": "Simple JSON server.",
  "scripts": {
    "start": "json-server --watch ./db.json --port 3001 db.json"
  },
  "private": true,
  "devDependencies": {
  },
  "dependencies": {
    "json-server": "^0.12.0"
  }
}
